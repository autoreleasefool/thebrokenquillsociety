<div class='index'>
  <div id='banner'>
    <div class='banner-inner'>
      <h1><%= UntitledCWC::Application::WEBSITE_NAME %></h1>
      <h2>Creative Writing at uOttawa</h2>
    </div>
    <div class='about-link'>
      <h4 class='title centered plain-link'><%= link_to 'Learn more', about_path %></h4>
    </div>
  </div>

  <div id='recent-works' class='centered-content'>
    <h2 class='title'>Recent submissions</h2>

    <% @recent_works.each do |work| %>
      <h3 class='title'><%= link_to work.title, work %></h3>
      <p class='author'>
        <strong>
          <% unless work.user.blank? %>
            <%= link_to work.user.name, user_path(work.user) %>
          <% else %>
            <span class='time'>[Deleted]</span>
          <% end %>
        </strong> <span class='time'> - <%= time_ago_in_words(work.created_at) %> ago</span>
      </p>
      <p class='excerpt'>
        <%= work.body[0..255] %>...
      </p>

      <% if work != @recent_works.last %>
        <hr />
      <% end %>
    <% end %>
  </div>
</div>
