<% @recent_works.each do |work| %>
  <p><%= link_to work.title, work %></p>
  <p><%= work.created_at.strftime('%B, %d, %Y') %></p>
<% end %>

<hr />

<% @recent_users.each do |user| %>
  <p><%= link_to user.name, user %></p>
  <p><%= user.created_at.strftime('%B, %d, %Y') %></p>
<% end %>
